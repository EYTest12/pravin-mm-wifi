# These are meant to work in both Python 2 and 3, except where noted.

# See my useful_pandas_snippets.py for those related to dataframes (such as pickling/`df.to_pickle(save_as)`)
# https://gist.github.com/fomightez/ef57387b5d23106fabd4e02dab6819b4
# also see https://gist.github.com/fomightez/324b7446dc08e56c83fa2d7af2b89a33 for examples of my 
# frequently used Python functions and slight variations for more expanded, modular structures.


#argparse
# good snippet collection at https://mkaz.tech/code/python-argparse-cookbook/
# positional
parser.add_argument("input", help="Name of the file that was \
    generated by other program \
    when run with your transcriptome of interest.", metavar="INPUT_FILE")

# with optional positional
parser.add_argument("input", nargs='?', help="**OPTIONAL**Name of the file \
    generated by other program \
    when run with your transcriptome of interest. Usually, this is \
    '"+input_file_name_default+"' &\
    if no input file name is provided then this will be used by \
    default.", default=input_file_name_default, metavar="INPUT_FILE")
# Note see https://stackoverflow.com/questions/18862836/how-to-open-file-using-argparse#comment35222484_18863004
# for why not using `argparse.FileType` approach here.
# See
# https://stackoverflow.com/questions/4480075/argparse-optional-positional-arguments 
# and 
# https://docs.python.org/2/library/argparse.html#nargs for use of `nargs='?'` 
# to make input and output file names optional. Note that the square brackets
# shown in the usage out signify optional according to 
# https://stackoverflow.com/questions/4480075/argparse-optional-positional-arguments#comment40460395_4480202
# , but because placed under positional I added clarifying text to help 
# description.
# IF MODIFYING THIS SCRIPT FOR USE ELSEWHERE AND DON'T NEED/WANT THE OUTPUT 
# FILE TO BE OPTIONAL, remove `nargs` (& default?) BUT KEEP WHERE NOT
# USING `argparse.FileType` AND USING `with open` AS CONSIDERED MORE PYTHONIC.

# With list where won't know the exact size of list, i,e. could be one (Example from `plot_expression_across_chromosomes.py`; see `donut_plot_with_subgroups_from_dataframe.py` for another)
parser.add_argument('-chr', '--chrs', action='store', type=str, 
    help="use this flag to limit plotting of the data to particular \
    chromosomes or scaffolds you specify immediately following this flag. \
    Separate the chromosome or scaffold identifiers by commas, without spaces. \
    Example use in a command is `--chrs I,IV,XVI`. \
    Default when this optional flag is not called is to plot that data for all \
    chromosomes or scaffolds. ") # based on
    # https://stackoverflow.com/questions/15753701/argparse-option-for-passing-a-list-as-option
    # ; specifically, https://stackoverflow.com/a/24866869/8508004
#...
if args.chrs:
if "," in args.chrs:
    limit_to_chrs = args.chrs.split(',')
else:
    # means only one item
    limit_to_chrs = [args.chrs] #has to be a list for passing to Pandas `isin()`

# with flag parameters
parser.add_argument('-sa', '--save_as', action='store', type=str, 
    default= generate_output_file_name(previous_pickled_df), help="Use \
    this option to supply a name of \
    the file to save for storing produced dataframe. If none is provided, \
    the name \'"+generate_output_file_name(previous_pickled_df)+"' will be \
    used. To force nothing to be saved, enter \
    `-sa no_output` without quotes as output file (ATYPICAL).") 
# with choices
# see https://stackoverflow.com/a/35970231/8508004 
# or https://stackoverflow.com/a/15301183/8508004
# or https://stackoverflow.com/questions/40324356/python-argparse-choices-with-a-default-choice
parser.add_argument("-og", "--output_grouping", type=str, 
        default= "single", choices=["single", "separate", "both"],
        help="OPTIONAL: Specify grouping of output with this option. Choose \
        `-og single` for one table or dataframe for all categories. Or choose \
        `-og separate` for a separate table or dataframe for each category. \
        Or specify `-og both` to output both types. \
        If this option is not specified, {} will be used.".format("single"))

# Now DISFAVORED approach for reading in files. (disfavored because use of `with` favored and not compatible with that. So only use when really in a hurry to scrape something together; see https://stackoverflow.com/questions/18862836/how-to-open-file-using-argparse#comment35222484_18863004)
parser.add_argument("MTA", help="Name of file containing data. REQUIRED.", type=argparse.FileType('r'), metavar="FILE")
#I would also like trigger help to display if no arguments provided because need at least one input file
if len(sys.argv)==1:    #from http://stackoverflow.com/questions/4042452/display-help-message-with-python-argparse-when-script-is-called-without-any-argu
    parser.print_help()
    sys.exit(1)
args = parser.parse_args()
the_file = args.MTA
# open input file and start reading
sys.stderr.write("\nReading input file...")
#input_file_stream = open(the_file, "r") # Don't need separate open when use `type=argparse.FileType`. It sets everything up automatically and you will actually cause errors if try to open when already open.
for line in thefile:
    pass

  


# assert
assert len(numbers_given_for_start_n_end) == len(aln_ids_in_out_order), (
    "The user-supplied list must be equal in length to list of data" 
    "previously supplied as 'aln_ids_in_out_order'.")
# Verify that values in dictionary are equal in length using `count` method of Python lists
lengths_of_sequences = [len(v) for v in sequence_dict.values()]
assert lengths_of_sequences.count(
    lengths_of_sequences[0]) == len(lengths_of_sequences), "The length "
    "of all parsed sequences should be the same." # see where 
    # that assertion test involving Ivo van der Wijk's solution from 
    # https://stackoverflow.com/a/3844948/8508004


# Getting hex color codes and RGB values from out of Seaborn color palettes / show colors
# based on https://stackoverflow.com/questions/38249454/extract-rgb-or-6-digit-code-from-seaborn-palette
# WORKS IN A JUPYTER NOTEBOOK CELL
import seaborn as sns
#num_shades = 8
#sns.palplot(sns.cubehelix_palette(num_shades))
pal = sns.color_palette("RdBu_r")
print(pal.as_hex())
print(pal)
sns.palplot(sns.color_palette("RdBu_r"))
sns.palplot(sns.diverging_palette(5, 250))


# Check if string can be cast to number and cast (example from `donut_plot_with_subgroups_from_dataframe.py`)
def is_number(s):
    '''
    check if a string can be cast to a float or numeric (integer).

    Takes a string.

    Returns True or False
    fixed from https://www.pythoncentral.io/how-to-check-if-a-string-is-a-number-in-python-including-unicode/
    later noted similar code is at https://code-maven.com/slides/python-programming/is-number
    '''
    try:
        float(s)
        return True
    except ValueError:
        pass
    try:
        import unicodedata
        unicodedata.numeric(s)
        return True
    except (TypeError, ValueError):
        pass
    return False

def cast_to_number(s):
    '''
    Cast a string to a float or integer. 
    Tries casting to float first and if that works then it tries casting the 
    string to an integer. (I thought I saw suggestion of that order somewhere 
    when searching for what I used as `is_number()` check but cannot find source
    right now.)

    Returns a float, int, or if fails, False. (Where using, it shouldn't ever
    trigger returning `False` because checked all could be converted first.)

    based on fixed code from https://www.pythoncentral.io/how-to-check-if-a-string-is-a-number-in-python-including-unicode/
    '''
    try:
        number = float(s)
        try:
            number = int(s)
            return number
        except ValueError:
            pass
        return number
    except ValueError:
        pass
    try:
        import unicodedata
        num = unicodedata.numeric(s)
        return num
    except (TypeError, ValueError):
        pass
    return False





# count frequency
def count_frequency_in_list(l):
    '''
    takes a list and returns a dictionary of the counts of the items in that 
    list

    based on https://stackoverflow.com/a/2162045/8508004
    '''
    import collections
    return collections.Counter(l)

clusters_nofeat = clusters_nofeat_df["Acluster(+)"].tolist() + clusters_nofeat_df["Acluster(-)"].tolist()
# split up those that contain more than one, i.e., separated by comma, and strip whitespace;
# expanded list comprehension from https://stackoverflow.com/a/27886807/8508004
clusters_nofeat = [x.strip() for xs in clusters_nofeat for x in xs.split(',')]
# remove the blank ones /empty ones (this is how you remove empty strings from a list) ; it can also be used to remove blank likes from splits on `"\n"`
clusters_nofeat = [x for x in clusters_nofeat if x]
# remove duplicates
clusters_nofeat = set(clusters_nofeat)
# remove the dash and everything after to count frequency
clusters_nofeat = [x.split("-")[0] for x in clusters_nofeat]
# count frequency
counts=count_frequency_in_list(clusters_nofeat)
clusters_nofeat
# next line based on #based on https://stackoverflow.com/questions/2161752/how-to-count-the-frequency-of-the-elements-in-a-list#comment46593992_2162045
# ; otherwise get `Counter()` type
dict(counts) 


most_common,num_most_common = Counter(some_list).most_common(1)[0] # based on
# https://stackoverflow.com/a/6987358/8508004

#unique items in list that occur more than two times (or some other number you specify)
the_count = Counter(some_list)
print ([k for k, v in the_count.items() if v > 2]) # based
# on https://stackoverflow.com/a/26773120/8508004 and
# https://stackoverflow.com/a/30418498/8508004 , to work in 2.7 and 3


# incrementing count by instance while interating on a list (could be applied to a dataframe column too)
# based on https://stackoverflow.com/a/1692428/8508004
from collections import defaultdict
l = ['apple','cherry','apple','cherry','cherry','pear']
d = defaultdict(int)
for x in l:
    d[x] += 1
    print ("That is {} #{}".format(x,d[x]))
print("final counts: {}".format(dict(d)))

# count the number of non-overlapping occurrences of a substring 
num_sequences = file_listing_text.count('.fa')



#combinining counting and regular expressions (regex)
# count frequency of blocks of Ns in a string (presumably sequence)
import re
from collections import defaultdict
t = "NaaNNNhcTCaaNANANDANNNNNNAANNANNANNNNNNNNANANANNANNNNNN"
matches = []
len_match_dict = defaultdict(int)
min_number_Ns_in_row_to_collect = 1
pattern_obj = re.compile("N{{{},}}".format(min_number_Ns_in_row_to_collect), re.I)  # adpated from
# code worked out in `collapse_large_unknown_blocks_in_DNA_sequence.py`, which relied heavily on
# https://stackoverflow.com/a/250306/8508004
for m in pattern_obj.finditer(t):
    len_match_dict[len(m.group())] += 1
    matches.append(m.group())
print(len_match_dict)
print(collections.Counter(matches))






#copy a list
# Important if you'll be itersting on it a modifying it at same time. Modify the copy or iterate on the copy but not both.
# Also useful if you want to start a new list with contents of old. If you just do `new_list = old_list`, that just copies 
# the reference to the list, see https://stackoverflow.com/a/2612815/8508004 . Won't probably be what you want.
new_list = old_list.copy()

# Split a list into chunks with also collecting any remainder group
# from https://stackoverflow.com/a/312464/8508004

def chunks(a_list, n):
    """Yield successive n-sized chunks from list."""
    for i in range(0, len(a_list), n):
        yield a_list[i:i+n]


print (list(chunks(range(10, 75), 10)))

# there is another approach with list comprehension for a string in this gist that is based 
# on https://stackoverflow.com/a/13673133/8508004 and looks similar to list comprehension one for
# lists at https://stackoverflow.com/a/32467096/8508004

# RELATED: chunking with step/stride of certain amount to produce overlap. For example to reproduce process
# described in this figure legend: " The data were calculated from 150-base pair windows with 50 base pairs of overlap between adjacent windows and plotted ..."
a = "adhaskjhdjkashdajkshdkjashdaslalllslslslsaashdahs"
chunk_size = 10
step_size = 5
def chunk_string_with_different_step(string, chunk_size, step_size):
    """Return a list of n-sized chunks from string of letters."""
    return [string[i:i+chunk_size] for i in range(0, len(string),step_size)] 
chunk_string_with_different_step(a,chunk_size,step_size)
'''
#RESULT:
['adhaskjhdj',
 'kjhdjkashd',
 'kashdajksh',
 'ajkshdkjas',
 'dkjashdasl',
 'hdaslallls',
 'alllslslsl',
 'lslslsaash',
 'saashdahs',
 'dahs']
'''
# AS GENERATOR:
b = "adhaskjhdjkashdajkshdkjashdaslalllslslslsaashdahs"
def gen_chunk_string_with_different_step(a_list, chunk_size, step_amount):
    """Yield successive n-sized chunks from list, stepping /stride by step_amount."""
    for i in range(0, len(a_list), step_amount):
        yield a_list[i:i+chunk_size]
print (list(gen_chunk_string_with_different_step(b,chunk_size,step_size)))
'''
#RESULT:
['adhaskjhdj', 'kjhdjkashd', 'kashdajksh', 'ajkshdkjas', 'dkjashdasl', 'hdaslallls', 'alllslslsl', 'lslslsaash', 'saashdahs', 'dahs']
'''






# Debugging
# Insert the following into a script at the point you'd like to debug to bring up interactive IPyton console 
# you can query for status of defined variables:
from IPython import embed; embed()







#dictionary
for key in d:
    pass

for key, value in d.items():
    pass

if key in d:
    pass

# some example dictionary comprehension ( general idea: d = {k:v for k,v in a.items()}  ) are at 
# https://stackoverflow.com/questions/1031851/python-best-way-to-exchange-keys-with-values-in-a-dictionary
# in answer to "Python: Best Way to Exchange Keys with Values in a Dictionary?"

# merge two dictionaries --> see https://www.geeksforgeeks.org/python-merging-two-dictionaries/
d = {**dict1, **dict2}    # or d = (dict2.update(dict1))


# do-while loop (https://stackoverflow.com/a/1662176/8508004)
while True:
    do_something()
    if condition():
        break

        
# get file extension from file name
# Now I'd use Pathlib, see https://docs.python.org/3/library/pathlib.html `Path(filename_n_path).suffix`
#; for main part using `Path(filename_n_path).stem` see more about Pathlib use in this document under 'Pathlib' below
def generate_output_file_name(file_name,suffix_for_saving):
    '''
    Takes a file name as an argument and returns string for the name of the
    output file. The generated name is based on the original file
    name.


    Specific example
    =================
    Calling function with
        ("sequence.fa", "_col")
    returns
        "sequence_col.fa"
    '''
    main_part_of_name, file_extension = os.path.splitext(
        file_name) #from 
    #http://stackoverflow.com/questions/541390/extracting-extension-from-filename-in-python
    if '.' in file_name:  #I don't know if this is needed with the os.path.splitext method but I had it before so left it
        return main_part_of_name + suffix_for_saving  + file_extension
    else:
        return file_name + suffix_for_saving + ".fa"



# Floor
import math
x = int(math.floor(10.4)) #outer `int()` typecast in Python 3 is redundant
print (x)
# outer typecast insures same result (an integer) is returned in both 
# Python 2 and 3 since Python 2 floor returns a float
# Not could get same by typecast to integer alone but use of 'floor' makes it more explicit as to what was sought.

# Get HTML / URL in Python 2 or 3
# Getting html originally for just Python 3, adapted from 
# https://stackoverflow.com/a/17510727/8508004 and then updated from to 
# handle Python 2 and 3 according to same link.
# (snippet with bonus Python 2 and 3 compatible variable unpacking and unicode decoding)
url = "http://www.example.org"
try:
    # For Python 3.0 and later
    from urllib.request import urlopen
except ImportError:
    # Fall back to Python 2's urllib2
    from urllib2 import urlopen
html = urlopen(url)
for line in html.read().splitlines():
    #name, chrom_len, *_ = line.strip().split()
    # that elegant unpack above is based on 
    # https://stackoverflow.com/questions/11371204/unpack-the-first-two-elements-in-list-tuple
    # , but it won't work in Python 2. From same place, one that works in 2:
    name, chrom_len = line.strip().split()[:2]
    chrom_and_length[name.decode(
        encoding='UTF-8')] = chrom_len.decode(encoding='UTF-8')

# generate names for sub-groups automatically,  à la `subset_a`, `subset_b`, etc.
first_group_suffix = "a"
groupings = ["subset"+ chr(ord(first_group_suffix) + x) for x in range(groups_to_make)]
# `chr(ord(first_group_suffix) + x)` part of line above based on https://stackoverflow.com/a/2156898/8508004

    
    
# greater than or equal and less than or equal
assert seq_step_size <= shortest_feature_len, ("problem")
# >= for greater than or equal
# <= for less than or equal
# related use in interval comparison:
if 10000 <= number <= 30000:
    pass


# check if any items in two lists are shared (any overlap / overlapping?)
bool(set(a) & set(b))





#list comprehension if and if-else conditionals
[x+1 if x >= 45 else x+5 for x in l]
# FOR JUST IF:
#"The if should be after the for (unless it is in an if-else ternary operator)
[y for y in a if y not in b]
#This would work however:
[y if y not in b else other_value for y in a]
#from https://stackoverflow.com/a/15474969/8508004
#My edited version of "just if" variation:
[x for x in list_for_order if x in b]

# List comprehension to add two items for each initial item in a list
# This example adds the number in the list plus the next value in the series
# for each item in a list.
#based on Games Brainiac comment at https://stackoverflow.com/a/19466238/8508004 and DSM's 
# comment at https://stackoverflow.com/a/11869360/8508004
l = [1,2,4]
[item for x in l for item in [x, x+1]]   # results in `[1, 2, 2, 3, 4, 5]`
# see related information about `s.shift()` for getting a row and the next one in pandas snippets

# plus see some nice list comprehensions in the 'count frequency' section above
# for the `count_frequency_in_list()` function
# plus there are these:
#remove all the blanks, i.e. `''` entries, in the sub-lists, and then
element_record[0] = [x for x in element_record[0] if x]
element_record[1] = [x for x in element_record[1] if x]
#join the contents that remain with commas to make a single string of ids
element_record[0] = ", ".join(element_record[0])
element_record[0] = ", ".join(element_record[1])
# -or- in one step, DO BOTH
#remove all the blanks, i.e. `''` entries, in the sub-lists, and then
#join the contents that remain with commas to make a single string of ids
element_record[0] = ", ".join([x for x in element_record[0] if x])
elementr_record[1] = ", ".join([x for x in element_record[1] if x])
# make a copy of a list with a specific item removed
first_characters_wo_candidate = [x for x in first_characters if x != basic_tag] #based
# on https://stackoverflow.com/a/25004389/8508004 (this will remove ALL occurences);
# I couldn't find a way to both make a copy of the list and remove the first instance of that 
# item using `.remove(item)` using Python 2.7. Always had to copy list and then use remove. Although
# potentially this looks like one line if have numpy already imported --> https://stackoverflow.com/a/50313691/8508004
 


# " one-liner providing both the value of the minimum as well as the first index where it is realized"
# from https://coderwall.com/p/a9hvrg/index-of-minimum-element-of-a-list where describes
# works because the default order of tuples in Python is lexicographical.
# Approach works for `max` (maximum) too.
# Needed slight adjusting to not use xrange for Python 3/2 compatibility.
l = [33, 788, 1, -14, 78, 11, 32, 11, 78, -1, -14]
mn,idx = min( (l[i],i) for i in range(len(l)) )
mn,idx


# next() with intertools
from itertools import cycle
#...
colors = (['C0', 'C1', 'C2', 'C3', 'C4', 'C5', 'xkcd:magenta', 'xkcd:orange', 
  'tab:gray','tab:pink'])
  #...
colors = cycle(colors)
#...
color = next(colors)
# works in 2 and 3, see https://stackoverflow.com/questions/5237611/itertools-cycle-next



# Dealing with detecting if something was in region/ interval
total_feature_levels = max([v["level"] for v in features_dict.values()])
#...
loc_intvls_of_features = {(
v["start"],v["end"]): k for k,v in features_dict.items()} #dict of the intervals
# (keys) of sequence where features(values) are located
#...
# Use `loc_intvls_of_features` and get list of features where the
# intervals contain what corresponds to the current column. Three
# categories: at_start, in_mid_interval, at_end. Could sort later
# for coordinating stylistic settings but might as well do now
# since all information at hand.
at_start=[]
in_mid_interval = []
at_end = []
for interval,feature in loc_intvls_of_features.items():
    if interval[0] == curr_residue_number:
        at_start.append(feature)
    elif interval[0] < curr_residue_number < interval[1]:
        in_mid_interval.append(feature)
    elif interval[1] == curr_residue_number :
        at_end.append(feature)
# Next if there are contents in the feature lists, cycle through them
# stylizing table cells of current column appropriately.
for feature in at_start:
    stylize_start_features(feature, column, features_part)
for feature in in_mid_interval:
    stylize_mid_features(feature, column, features_part)
for feature in at_end:
            stylize_end_features(feature, column, features_part)

def overlap_exists(a, b):
    '''
    takes two intervals and returns if they overlap. The intervals would
    be defined by tuples of (start, stop).

    Examples:
    overlap_exists([20, 38],[1, 125])
    > True

    overlap_exists([10, 15], [20, 38])
    > False

    overlap_exists([10, 25], [20, 38])
    > True

    overlap_exists([10, 25], [25, 38])
    > False

    based on https://stackoverflow.com/a/2953979/8508004 and fact 0 is same
    as False and anything else is True.
    Modified to add the +1 because I want to be inclusive so even one 
    shared basepair is an overlap
    '''
    return bool(max(0, (min(a[1], b[1]) - max(a[0], b[0])+1)))

    
    
# left justify by adding spaces. Really useful when creating multiple sequence alignments to control columns things show up in.
# based on https://stackoverflow.com/a/5676676/8508004
mviewlines_dict[i][0] = mviewlines_dict[i][0].ljust(len_longest_id)
    
    
# Minimum size of string in a list
print len(min(lizt_o_strings, key=len)) #based on https://stackoverflow.com/a/7228951/8508004
    
    
# operating system/shell dealings    
# os.remove() to delete a file

# make a directory
import os, errno

try:
    os.makedirs(directory)
except OSError as e:
    if e.errno != errno.EEXIST:
        raise
# move a file
shutil.move("path/to/current/file.foo", "path/to/new/destination/for/file.foo")
#for more see [Replacing Bash Scripting with Python](https://github.com/ninjaaron/replacing-bash-scripting-with-python)

#see `fnmatch` examples in the useful notebook snippets gist.
# see `glob.glob` examples of matching file extensions and filenames, etc., in the useful notebook snippets gist.





# Pathlib
# in addition to this see `pathlib` examples in useful notebook snippets gist.
#Pathlib in Python 2 or 3 example:
try:
    from pathlib import Path
except ImportError:
    from pathlib2 import Path
# list all files in a directory 
[item for item in Path('.').glob('*')] # based on 
# https://jefftriplett.com/2017/pathlib-is-wonderful/
# list final file extension , see 'Path(filenmae_n_path).suffix' at 
#https://docs.python.org/3/library/pathlib.html
[item.suffix for item in Path('.').glob('*')]
# list the final suffixes if there is more than one - see 'Path.suffixes' at 
#https://docs.python.org/3/library/pathlib.html
# main part without the extension is 'Path(filenmae_n_path).stem'
    
    
    
    
# Perform a function or carry out a calculation a certain percent of the time (i.e., with a random frequency)
import random
if random.random() < 0.8:
    do_something()
# example using it:
def get_unique_tag(string, length_of_tags):
    '''
    Takes a string and generates a unique tag of provided length
    related to the provided string.
    Alphanumerics in tag will be limited to those in the provided
    string. Although the same order is favored, there
    is a chance of returning characters out of order using
    shortuuid.
    shortuuid from https://github.com/skorokithakis/shortuuid
    '''
    import random
    if random.random() < 0.95:
        chunk_size = length_of_tags
        chunks = [string[i:i+chunk_size] for i in range(0, len(string),chunk_size)] #based on
        # https://stackoverflow.com/a/13673133/8508004 or https://stackoverflow.com/a/9475354/8508004
        '''
        # WRITEN AS A FUNCTION
        def chunk_string(string, chunk_size):
            """Return a list of n-sized chunks from string of letters."""
            return [string[i:i+chunk_size] for i in range(0, len(string),chunk_size)] 
        '''
        # discard chunks too short
        chunks = [x for x in chunks if len(x)== length_of_tags]
        import random
        return random.choice(chunks).lower()
    else:
        set_alphabet(string)
        return uuid()[:length_of_tags].lower()  
    
# Pickle Python objects for storage
# for simple objects json suggested by Martijn Pieters at https://stackoverflow.com/q/25464295/8508004
# as lighter-weight and more portable than Python's pickling. (Same page has a similar pickling
# example actually too under Mike McKerns answer. Good for a list of strings, for example:
# Save as json
import json
with open('filename_list.json', 'w') as f:
    json.dump(CTD_seqs_fn_list, f)

#Read as json
import json
with open('filename_list.json', 'r') as f:
    filename_list = json.load(f)


# Subset to a random sampling of items in a list , based on https://pynative.com/python-random-sample/
import random
genomes = random.sample(population=genomes, k=15)

    
# Read and write to a file
# prepare output file for saving so it will be open and ready
# NOTE ABOUT THE READING PART OF THIS: seems more modern Pythonic way
# is to leave out the `,'r'` part. See https://stackabuse.com/read-a-file-line-by-line-in-python/ under
# 'Read a File Line-by-Line with a for Loop - Most Pythonic Approach'. Note also that
# best to use `.strip()` or possibly slice `[:-1]` to remove line ending if going to 
# rearrange because can get weird merge if alter order because usually last line will not have a new
# line character. Also see https://stackoverflow.com/a/3277516/8508004 for code that will read the entire file
# into memory, or  or line by line ,and remove all whitespace characters (newlines and spaces) from the end of each line.
with open(output_file_name, 'w') as output:

    # read in the input file; REMAMEBER BEST NOT TO USE `with open(input_file_name, 'r') as input:` BECAUSE `input()` is something in python you may want to use in your script later
    with open(input_file_name, 'r') as input_handle: # OR SEE NOTE ABOVE HOW DON'T NEED `, 'r'` anymore.
        # prepare to give feeback later or allow skipping to certain start
        lines_processed = 0
        # This gff3 doesn't have pertinent lines until line 10

        for line in input_handle:
            lines_processed += 1
            # This gff3 doesn't have pertinent lines until line 10
            if line.startswith("#") or lines_processed < 9:
                # Send text to output
                output.write(line)
            else:
                
                info = line.split("\t")
                #print(info)
                info[3] = str(adjust_pos(int(info[3]),ATP6_start_pos, chromosome_length))
                info[4] = str(adjust_pos(int(info[4]),ATP6_start_pos, chromosome_length))
                #print (info) # ONLY FOR DEBUGGING
                
                # Send text to output
                output.write(("\t").join(info))
# Feedback
sys.stderr.write("Positions were changed to match ATP6 as start "
    "and saved in '{}'.".format(output_file_name))
  
# Read entire file into memory at once(extra bouns example removes linebreaks to make a a long string) /Read all file at once
with open('data.txt', 'r') as myfile:
    data=myfile.read().replace('\n', '')
    
# REplace text in a file combining much of above approaches
script_name = "donut_plot_with_subgroups_from_dataframe.py"
def change_original_title(s, script_name):
    '''
    Change the plot title to the provided text in the file designated with `script_name`
    '''
    with open(script_name, 'r') as thefile:
        script=thefile.read()
    script = script.replace('BREAKDOWN', s)
    with open(script_name, 'w') as output_file:
        output_file.write(script)
change_original_title("NEW TITLE GOES HERE", script_name) 
    

    
    
    
# Sort / Sorting    
# FOR PYTHON FOR GENOMIC DATA SCIENCE I WAS LOOKING TO SORT A REPRESENTATION OF A DICTIONARY BASED ON VALUES
# see http://stackoverflow.com/questions/613183/sort-a-python-dictionary-by-value
import operator
x = {1: 2, 3: 4, 4: 3, 2: 1, 0: 0}
sorted_x = sorted(x.items(), key=operator.itemgetter(1))

#Later I found is same without need for operator (and know to work in Python 3).:
sorted_keys = sorted(my_dict, key=my_dict.get) #based on https://stackoverflow.com/a/37270275/8508004
# to sort keys from dictionary based on value  
# see my file `sorting out sorting on attributes within lists using keys.md` for more along these lines
   


 
    
    
# StringIO 

try:
    from StringIO import StringIO
except ImportError:
    from io import StringIO
    
df = pd.read_table(StringIO(Input), header=0, delim_whitespace= True)
# verified for both Python 2 and 3


# stderr.write
import sys
sys.stderr.write("\n\nThe dataframe was not stored for use elsewhere "
            "because `no_output` was specified in place of the output file name.")
sys.stderr.write( "\n\nThe dataframe has been saved as a file in a "
                "manner where other Python programs can access\nthe created "
                "dataframe (pickled).\n"
                "The dataframe is stored as '{}'".format(out_name))



# String formatting
# my fav resources are https://pyformat.info/  and   https://mkaz.blog/code/python-string-format-cookbook/
## Example with named placeholders
next_line = ("chr\t-\t{species_code}-{chrom}\t{chrom}\t0"
                "\t{length}\tblack".format(
                species_code=species_code,chrom=chrom, length=length))
# see https://mkaz.blog/code/python-string-format-cookbook/ for percentage and Exponent notation, 
# although I tend to prefer capital E & that can easily be done by replacing the lowercase 
# one that is shown with upper case like:
print("{:.2E}".format(3.1415926)) #results in `3.14E+00`
print("{:.3E}".format(602213969921133261473164)) #results in `6.022E+23`
print("Half is {:.2%}".format(0.5000000)) #`.2%` limits to two decimals


# ternary operator if-or conditional (a if condition else b) for setting a variable
direction_string = "positive" if direction > 0 else "negative"



# Try except
try:
  return urlopen(url).read()
except HTTPError as e:
  #print e.code
  #print e.msg
  return "HTTPError"


# write to file
# prepare output file for saving so it will be open and ready
with open(output_file_name, 'w') as output_file:
    for indx,(chrom,length) in enumerate(chromosomes_and_length.items()):
        next_line = ("chr\t-\t{species_code}-{chrom}\t{chrom}\t0"
            "\t{length}\tblack".format(
            species_code=species_code,chrom=chrom, length=length))
        if indx < (len(chromosomes_and_length)-1):
            next_line += "\n" # don't add new line character to last line
        # Send the built line to output
        output_file.write(next_line)
# provide feedback
sys.stderr.write( "\n\nThe karyotype file for {} chromosomes has been saved "
        "as a file named"
        " '{}'.".format(len(chromosomes_and_length),output_file_name))

# also see 'Read and write to a file' above


# decoding and undecoding a string as base64, based on https://stackabuse.com/encoding-and-decoding-base64-strings-in-python/ (note could also use zlib (see https://stackoverflow.com/a/29243206/8508004) but it seemed like make things take up more space/looks messy in the script then the original table. Whereas base64 seemed a little less expansive and cleaner.)
# The idea of doing this was to make a table take up less lines of a script when hardcoding what would normally be data in a separate tsv file into a script. So hardcode it in but in less lines
import base64
message = wt_bendit_result
message_bytes = message.encode('ascii')
base64_bytes = base64.b64encode(message_bytes)
base64_message = base64_bytes.decode('ascii')
#base64_message is the string as based64

#UNDO CONVERSION
undecodedbase64 = base64.b64decode(base64_message)
# use `undecodedbase64 = undecodedbase64.decode()` after that if need 
# as a string and using `base64_message = '''dGhpcyBpcyBhIHRlc3Q='''` to
# hardcode iinto a script. because with just above that will produce bytes from
# `base64_message = '''dGhpcyBpcyBhIHRlc3Q='''` .
print(base64_message)
```


#Suppressing stdout / stderr when using imported function or subprocess, etc.
#Vaguely similar to using `%%capture` to hush noisy output /code in notebooks.
#This is very useful when you are importing a function or running internal code as a subprocess and what it is saying in stderr feedback may be wrong or moot because of the way you are processing what function returns.
# based on https://stackoverflow.com/a/52442331/8508004
# also see `bendIt_analysis.py` where I do it slightly differently (with `with io.capture_output() as captured:`) 
# within a python script because running in a juptyer environment. 

from contextlib import contextmanager,redirect_stderr,redirect_stdout
from os import devnull

@contextmanager
def suppress_stdout_stderr():
    """
    A context manager that redirects stdout and stderr to devnull.
    From https://stackoverflow.com/a/52442331/8508004
    """
    with open(devnull, 'w') as fnull:
        with redirect_stderr(fnull) as err, redirect_stdout(fnull) as out:
            yield (err, out)

#...
with suppress_stdout_stderr():
    pm_df = patmatch_results_to_df(result)


# Python version of curl is in useful_ipython_to_python_snippets.py at https://gist.github.com/fomightez/ed79e33e97601d839dd550fd224d583c

# Python version of checking software to run in shell is installed in environment is in useful_ipython_to_python_snippets.py at https://gist.github.com/fomightez/ed79e33e97601d839dd550fd224d583c

# ===== AUTO-GENERATED PADDING BELOW =====

# ------------------------------
# Random helper block 0
# Auto-generated padding code
def _dummy_function_0(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 0, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_0 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 1
# Auto-generated padding code
def _dummy_function_1(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 1, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_1 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 2
# Auto-generated padding code
def _dummy_function_2(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 2, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_2 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 3
# Auto-generated padding code
def _dummy_function_3(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 3, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_3 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 4
# Auto-generated padding code
def _dummy_function_4(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 4, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_4 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 5
# Auto-generated padding code
def _dummy_function_5(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 5, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_5 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 6
# Auto-generated padding code
def _dummy_function_6(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 6, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_6 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 7
# Auto-generated padding code
def _dummy_function_7(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 7, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_7 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 8
# Auto-generated padding code
def _dummy_function_8(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 8, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_8 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 9
# Auto-generated padding code
def _dummy_function_9(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 9, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_9 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 10
# Auto-generated padding code
def _dummy_function_10(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 10, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_10 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 11
# Auto-generated padding code
def _dummy_function_11(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 11, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_11 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 12
# Auto-generated padding code
def _dummy_function_12(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 12, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_12 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 13
# Auto-generated padding code
def _dummy_function_13(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 13, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_13 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 14
# Auto-generated padding code
def _dummy_function_14(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 14, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_14 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 15
# Auto-generated padding code
def _dummy_function_15(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 15, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_15 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 16
# Auto-generated padding code
def _dummy_function_16(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 16, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_16 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 17
# Auto-generated padding code
def _dummy_function_17(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 17, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_17 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 18
# Auto-generated padding code
def _dummy_function_18(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 18, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_18 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 19
# Auto-generated padding code
def _dummy_function_19(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 19, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_19 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 20
# Auto-generated padding code
def _dummy_function_20(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 20, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_20 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 21
# Auto-generated padding code
def _dummy_function_21(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 21, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_21 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 22
# Auto-generated padding code
def _dummy_function_22(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 22, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_22 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 23
# Auto-generated padding code
def _dummy_function_23(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 23, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_23 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 24
# Auto-generated padding code
def _dummy_function_24(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 24, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_24 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 25
# Auto-generated padding code
def _dummy_function_25(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 25, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_25 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 26
# Auto-generated padding code
def _dummy_function_26(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 26, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_26 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 27
# Auto-generated padding code
def _dummy_function_27(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 27, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_27 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 28
# Auto-generated padding code
def _dummy_function_28(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 28, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_28 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 29
# Auto-generated padding code
def _dummy_function_29(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 29, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_29 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 30
# Auto-generated padding code
def _dummy_function_30(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 30, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_30 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 31
# Auto-generated padding code
def _dummy_function_31(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 31, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_31 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 32
# Auto-generated padding code
def _dummy_function_32(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 32, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_32 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 33
# Auto-generated padding code
def _dummy_function_33(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 33, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_33 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 34
# Auto-generated padding code
def _dummy_function_34(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 34, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_34 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 35
# Auto-generated padding code
def _dummy_function_35(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 35, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_35 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 36
# Auto-generated padding code
def _dummy_function_36(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 36, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_36 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 37
# Auto-generated padding code
def _dummy_function_37(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 37, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_37 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 38
# Auto-generated padding code
def _dummy_function_38(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 38, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_38 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 39
# Auto-generated padding code
def _dummy_function_39(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 39, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_39 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 40
# Auto-generated padding code
def _dummy_function_40(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 40, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_40 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 41
# Auto-generated padding code
def _dummy_function_41(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 41, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_41 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 42
# Auto-generated padding code
def _dummy_function_42(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 42, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_42 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 43
# Auto-generated padding code
def _dummy_function_43(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 43, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_43 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 44
# Auto-generated padding code
def _dummy_function_44(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 44, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_44 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 45
# Auto-generated padding code
def _dummy_function_45(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 45, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_45 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 46
# Auto-generated padding code
def _dummy_function_46(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 46, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_46 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 47
# Auto-generated padding code
def _dummy_function_47(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 47, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_47 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 48
# Auto-generated padding code
def _dummy_function_48(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 48, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_48 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 49
# Auto-generated padding code
def _dummy_function_49(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 49, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_49 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 50
# Auto-generated padding code
def _dummy_function_50(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 50, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_50 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 51
# Auto-generated padding code
def _dummy_function_51(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 51, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_51 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 52
# Auto-generated padding code
def _dummy_function_52(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 52, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_52 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 53
# Auto-generated padding code
def _dummy_function_53(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 53, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_53 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 54
# Auto-generated padding code
def _dummy_function_54(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 54, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_54 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 55
# Auto-generated padding code
def _dummy_function_55(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 55, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_55 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 56
# Auto-generated padding code
def _dummy_function_56(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 56, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_56 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 57
# Auto-generated padding code
def _dummy_function_57(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 57, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_57 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 58
# Auto-generated padding code
def _dummy_function_58(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 58, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_58 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 59
# Auto-generated padding code
def _dummy_function_59(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 59, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_59 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 60
# Auto-generated padding code
def _dummy_function_60(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 60, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_60 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 61
# Auto-generated padding code
def _dummy_function_61(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 61, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_61 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 62
# Auto-generated padding code
def _dummy_function_62(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 62, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_62 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 63
# Auto-generated padding code
def _dummy_function_63(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 63, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_63 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 64
# Auto-generated padding code
def _dummy_function_64(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 64, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_64 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 65
# Auto-generated padding code
def _dummy_function_65(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 65, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_65 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 66
# Auto-generated padding code
def _dummy_function_66(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 66, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_66 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 67
# Auto-generated padding code
def _dummy_function_67(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 67, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_67 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 68
# Auto-generated padding code
def _dummy_function_68(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 68, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_68 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 69
# Auto-generated padding code
def _dummy_function_69(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 69, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_69 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 70
# Auto-generated padding code
def _dummy_function_70(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 70, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_70 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 71
# Auto-generated padding code
def _dummy_function_71(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 71, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_71 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 72
# Auto-generated padding code
def _dummy_function_72(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 72, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_72 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 73
# Auto-generated padding code
def _dummy_function_73(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 73, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_73 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 74
# Auto-generated padding code
def _dummy_function_74(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 74, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_74 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 75
# Auto-generated padding code
def _dummy_function_75(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 75, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_75 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 76
# Auto-generated padding code
def _dummy_function_76(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 76, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_76 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 77
# Auto-generated padding code
def _dummy_function_77(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 77, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_77 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 78
# Auto-generated padding code
def _dummy_function_78(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 78, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_78 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 79
# Auto-generated padding code
def _dummy_function_79(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 79, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_79 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 80
# Auto-generated padding code
def _dummy_function_80(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 80, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_80 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 81
# Auto-generated padding code
def _dummy_function_81(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 81, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_81 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 82
# Auto-generated padding code
def _dummy_function_82(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 82, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_82 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 83
# Auto-generated padding code
def _dummy_function_83(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 83, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_83 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 84
# Auto-generated padding code
def _dummy_function_84(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 84, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_84 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 85
# Auto-generated padding code
def _dummy_function_85(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 85, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_85 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 86
# Auto-generated padding code
def _dummy_function_86(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 86, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_86 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 87
# Auto-generated padding code
def _dummy_function_87(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 87, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_87 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 88
# Auto-generated padding code
def _dummy_function_88(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 88, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_88 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 89
# Auto-generated padding code
def _dummy_function_89(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 89, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_89 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 90
# Auto-generated padding code
def _dummy_function_90(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 90, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_90 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 91
# Auto-generated padding code
def _dummy_function_91(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 91, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_91 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 92
# Auto-generated padding code
def _dummy_function_92(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 92, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_92 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 93
# Auto-generated padding code
def _dummy_function_93(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 93, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_93 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 94
# Auto-generated padding code
def _dummy_function_94(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 94, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_94 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 95
# Auto-generated padding code
def _dummy_function_95(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 95, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_95 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 96
# Auto-generated padding code
def _dummy_function_96(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 96, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_96 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 97
# Auto-generated padding code
def _dummy_function_97(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 97, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_97 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 98
# Auto-generated padding code
def _dummy_function_98(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 98, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_98 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 99
# Auto-generated padding code
def _dummy_function_99(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 99, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_99 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 100
# Auto-generated padding code
def _dummy_function_100(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 100, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_100 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 101
# Auto-generated padding code
def _dummy_function_101(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 101, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_101 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 102
# Auto-generated padding code
def _dummy_function_102(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 102, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_102 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 103
# Auto-generated padding code
def _dummy_function_103(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 103, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_103 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 104
# Auto-generated padding code
def _dummy_function_104(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 104, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_104 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 105
# Auto-generated padding code
def _dummy_function_105(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 105, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_105 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 106
# Auto-generated padding code
def _dummy_function_106(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 106, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_106 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 107
# Auto-generated padding code
def _dummy_function_107(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 107, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_107 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 108
# Auto-generated padding code
def _dummy_function_108(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 108, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_108 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 109
# Auto-generated padding code
def _dummy_function_109(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 109, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_109 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 110
# Auto-generated padding code
def _dummy_function_110(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 110, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_110 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 111
# Auto-generated padding code
def _dummy_function_111(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 111, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_111 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 112
# Auto-generated padding code
def _dummy_function_112(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 112, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_112 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 113
# Auto-generated padding code
def _dummy_function_113(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 113, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_113 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 114
# Auto-generated padding code
def _dummy_function_114(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 114, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_114 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 115
# Auto-generated padding code
def _dummy_function_115(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 115, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_115 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 116
# Auto-generated padding code
def _dummy_function_116(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 116, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_116 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 117
# Auto-generated padding code
def _dummy_function_117(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 117, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_117 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 118
# Auto-generated padding code
def _dummy_function_118(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 118, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_118 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 119
# Auto-generated padding code
def _dummy_function_119(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 119, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_119 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 120
# Auto-generated padding code
def _dummy_function_120(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 120, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_120 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 121
# Auto-generated padding code
def _dummy_function_121(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 121, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_121 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 122
# Auto-generated padding code
def _dummy_function_122(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 122, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_122 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 123
# Auto-generated padding code
def _dummy_function_123(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 123, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_123 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 124
# Auto-generated padding code
def _dummy_function_124(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 124, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_124 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 125
# Auto-generated padding code
def _dummy_function_125(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 125, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_125 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 126
# Auto-generated padding code
def _dummy_function_126(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 126, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_126 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 127
# Auto-generated padding code
def _dummy_function_127(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 127, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_127 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 128
# Auto-generated padding code
def _dummy_function_128(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 128, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_128 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 129
# Auto-generated padding code
def _dummy_function_129(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 129, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_129 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 130
# Auto-generated padding code
def _dummy_function_130(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 130, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_130 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 131
# Auto-generated padding code
def _dummy_function_131(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 131, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_131 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 132
# Auto-generated padding code
def _dummy_function_132(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 132, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_132 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 133
# Auto-generated padding code
def _dummy_function_133(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 133, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_133 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 134
# Auto-generated padding code
def _dummy_function_134(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 134, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_134 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 135
# Auto-generated padding code
def _dummy_function_135(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 135, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_135 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 136
# Auto-generated padding code
def _dummy_function_136(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 136, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_136 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 137
# Auto-generated padding code
def _dummy_function_137(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 137, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_137 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 138
# Auto-generated padding code
def _dummy_function_138(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 138, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_138 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 139
# Auto-generated padding code
def _dummy_function_139(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 139, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_139 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 140
# Auto-generated padding code
def _dummy_function_140(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 140, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_140 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 141
# Auto-generated padding code
def _dummy_function_141(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 141, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_141 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 142
# Auto-generated padding code
def _dummy_function_142(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 142, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_142 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 143
# Auto-generated padding code
def _dummy_function_143(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 143, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_143 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 144
# Auto-generated padding code
def _dummy_function_144(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 144, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_144 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 145
# Auto-generated padding code
def _dummy_function_145(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 145, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_145 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 146
# Auto-generated padding code
def _dummy_function_146(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 146, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_146 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 147
# Auto-generated padding code
def _dummy_function_147(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 147, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_147 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 148
# Auto-generated padding code
def _dummy_function_148(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 148, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_148 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 149
# Auto-generated padding code
def _dummy_function_149(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 149, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_149 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 150
# Auto-generated padding code
def _dummy_function_150(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 150, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_150 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 151
# Auto-generated padding code
def _dummy_function_151(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 151, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_151 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 152
# Auto-generated padding code
def _dummy_function_152(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 152, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_152 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 153
# Auto-generated padding code
def _dummy_function_153(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 153, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_153 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 154
# Auto-generated padding code
def _dummy_function_154(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 154, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_154 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 155
# Auto-generated padding code
def _dummy_function_155(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 155, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_155 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 156
# Auto-generated padding code
def _dummy_function_156(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 156, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_156 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 157
# Auto-generated padding code
def _dummy_function_157(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 157, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_157 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 158
# Auto-generated padding code
def _dummy_function_158(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 158, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_158 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 159
# Auto-generated padding code
def _dummy_function_159(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 159, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_159 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 160
# Auto-generated padding code
def _dummy_function_160(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 160, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_160 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 161
# Auto-generated padding code
def _dummy_function_161(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 161, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_161 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 162
# Auto-generated padding code
def _dummy_function_162(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 162, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_162 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 163
# Auto-generated padding code
def _dummy_function_163(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 163, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_163 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 164
# Auto-generated padding code
def _dummy_function_164(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 164, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_164 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 165
# Auto-generated padding code
def _dummy_function_165(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 165, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_165 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 166
# Auto-generated padding code
def _dummy_function_166(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 166, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_166 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 167
# Auto-generated padding code
def _dummy_function_167(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 167, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_167 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 168
# Auto-generated padding code
def _dummy_function_168(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 168, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_168 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 169
# Auto-generated padding code
def _dummy_function_169(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 169, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_169 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 170
# Auto-generated padding code
def _dummy_function_170(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 170, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_170 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 171
# Auto-generated padding code
def _dummy_function_171(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 171, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_171 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 172
# Auto-generated padding code
def _dummy_function_172(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 172, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_172 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 173
# Auto-generated padding code
def _dummy_function_173(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 173, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_173 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 174
# Auto-generated padding code
def _dummy_function_174(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 174, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_174 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 175
# Auto-generated padding code
def _dummy_function_175(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 175, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_175 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 176
# Auto-generated padding code
def _dummy_function_176(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 176, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_176 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 177
# Auto-generated padding code
def _dummy_function_177(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 177, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_177 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 178
# Auto-generated padding code
def _dummy_function_178(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 178, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_178 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]

# ------------------------------
# Random helper block 179
# Auto-generated padding code
def _dummy_function_179(x=None, y=None):
    """Auto-generated no-op function used only for padding."""
    if x is None and y is None:
        return {'index': 179, 'value': None}
    try:
        return (x, y, x == y, hash(str(x)) ^ hash(str(y)))
    except Exception:
        return None

_dummy_data_179 = [{'i': j, 'square': j*j, 'cube': j*j*j} for j in range(20)]
